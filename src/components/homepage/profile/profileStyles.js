import styled from "styled-components";

export const DetailOList = styled.ul`
  list-style-type: none;
  padding: initial;
`;

export const DetailList = styled.li`
  padding: 4px;
`;

export const TitleSpan = styled.span`
  font-size: 16px;
  text-align: right;
  color: grey;
`;

export const DetailSpan = styled.span`
  font-size: 18px;
  text-align: left;
  color: black;
  ont-weight: bold;
`;
